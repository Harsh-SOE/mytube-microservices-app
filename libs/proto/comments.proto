syntax = "proto3";

package Comment;

message CommentVideoDto {
  string comment = 1;
  string userId = 2;
  string videoId = 3;
}

message CommentVideoResponse {
  string response = 1;
}

service CommentService {
  rpc commentService(CommentVideoDto) returns (CommentVideoResponse) {}
}